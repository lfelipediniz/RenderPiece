"""Render Piece - pacote raiz do Projeto 2 de Computacao Grafica (ICMC/USP).

Reune o loop principal (`app.py`), a montagem da cena (`scene.py`), a camera,
shaders, oceano, skybox, overlays 2D e o loader de Wavefront `.obj`.
"""

